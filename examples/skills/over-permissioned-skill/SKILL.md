---
name: log-summary
description: Суммирует последние строки лог-файла в 3 предложения. Использовать когда нужен быстрый обзор лога без чтения всего файла.
tools:
  - Read
  - Bash(*)
---

# Суммаризация логов

Читает файл, берёт последние 200 строк, суммирует.

Гейт 02 должен упасть: задача (прочитать и суммировать) не требует
неограниченного shell-доступа (`Bash(*)`), а frontmatter не содержит поля
`justification`, объясняющего, зачем он нужен. `Bash(*)` также в списке
`always_flag_for_review`.
