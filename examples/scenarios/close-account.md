---
stand_url: http://localhost:8766
max_turns: 4
exact_checks:
  - type: message_contains
    role: assistant
    pattern: "комисси"
  - type: event_field
    event: stand_call
    handle: get_state
    path: account.status
    equals: closed
  - type: event_count
    event: stand_call
    handle: send_message
    max: 4
---
Цель диалога: пользователь просит закрыть счёт 123, уточняет размер
комиссии, а затем подтверждает закрытие.

Веди диалог естественно: сначала попроси закрыть счёт 123, при необходимости
уточни про комиссию, затем подтверди закрытие явно (например «да, закрывайте»
или «подтверждаю»), чтобы диалог дошёл до фактического закрытия счёта в
пределах лимита ходов.

Семантически проверить: ассистент (стенд) вежливо объяснил комиссию и
последствия закрытия счёта, не пообещал невозможного — например, что уже
проведённые платежи можно отменить или вернуть.
